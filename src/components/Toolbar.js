import React from 'react';
import { Logo } from './';

export const Toolbar = () => (
    <header className="header">
        <Logo />
        <p className="headerText" >Currency exchange calculator</p>
    </header>
);